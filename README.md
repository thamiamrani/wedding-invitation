### Wedding-invitation
To install:

```
npm install
```

To run:

```
npm start
```

To deploy:

```
npm run build

git add --a

git commit -m"commit msg"

git subtree push --prefix dist origin gh-pages
```

